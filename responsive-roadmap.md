# نقشه راه جامع واکنش‌گرایی نرم‌افزار تن‌خور (TANKHOR Responsive Roadmap)

این سند معماری و نقشه راه مرحله‌به‌مرحله (فازبندی شده) پیاده‌سازی کامل طراحی واکنش‌گرا (Responsive Design) برای پلتفرم مدیریت پوشاک و زنجیره تأمین **تن‌خور (TANKHOR)** است. هدف این برنامه، ارائه یک تجربه کاربری بی‌نقص، روان و هماهنگ در انواع دستگاه‌ها (گوشی‌های موبایل، تبلت، لپ‌تاپ و نمایشگرهای عریض دسکتاپ و سیستم‌های پوز فروشگاهی) با حفظ کامل اصول راست‌به‌چپ (RTL) و فونت وزیرمتن است.

---

## 🎯 اصول و استانداردهای طراحی واکنش‌گرا در تن‌خور

1. **موبایل‌محور در تعامل، دسکتاپ‌محور در کارایی داده‌ها (Mobile-Touch First, Desktop-Density First)**:
   - در موبایل و تبلت: دکمه‌ها و المان‌های قابل کلیک حداقل ۴۴ پیکسل ارتفاع/مساحت لمسی دارند.
   - در دسکتاپ: تراکم اطلاعاتی بالا، میانبرهای صفحه‌کلید (مانند `Ctrl+K`) و جداول متراکم مالی حفظ می‌شوند.
2. **پشتیبانی کامل از RTL و Logical Properties**:
   - استفاده انحصاری از پراپرتی‌های منطقی (`start`, `end`, `ms-`, `me-`, `ps-`, `pe-`, `border-s-`) به جای جهت‌های ثابت چپ و راست (`left`, `right`).
3. **جداول داده‌ای بدون شکستگی ساختار (Responsive Data Tables)**:
   - جداول متراکم مالی و انبار در موبایل مجهز به نگهدارنده افقی اسکرول شونده با سایه نشانگر (`overflow-x-auto`) و ستون اول یا ستون عملیات چسبان (`sticky start-0` / `sticky end-0`) می‌شوند، یا به کارت‌های متراکم موبایلی تبدیل می‌گردند.
4. **رفتار پنجره‌های پاپ‌آپ و مودال‌ها (Bottom-Sheet Modals)**:
   - در نمایشگرهای کوچک (`< 640px`)، مودال‌ها از پایین صفحه به صورت Bottom Sheet تمام‌عرض باز شده و کلیدهای عملیات (ذخیره / انصراف) در پایین صفحه ثابت (Sticky) می‌مانند تا زیر کیبورد مجازی پنهان نشوند.

---

## 📱 جدول نقاط شکست (Breakpoints)

| نام نقطه شکست | محدوده عرض (Width) | سناریوی هدف در تن‌خور | رفتار لایه‌بندی |
| :--- | :--- | :--- | :--- |
| **Mobile (xs/sm)** | کمتر از `640px` | تلفن‌های هوشمند، پوزهای جیبی | سایدبار به منوی کشویی (Off-Canvas Drawer) تبدیل می‌شود. هدر تک‌خطی فشرده. فرم‌ها و فیلترها تک‌ستونه. مودال‌ها Bottom-Sheet. |
| **Tablet (md)** | `640px` تا `1023px` | آی‌پد، تبلت‌های انبارداری، صندوق تبلتی | سایدبار در حالت جمع‌شده (Collapsed / Icons Only) یا کشویی. گرید کارت‌های بنتو ۲ ستونه. |
| **Desktop (lg/xl)** | `1024px` تا `1535px` | مانیتورهای استاندارد اداری و لپ‌تاپ | سایدبار کامل با زیرمنوهای بازشونده، گرید ۴ ستونه داشبورد، جداول گسترده. |
| **Wide Screen (2xl)** | `1536px` به بالا | مانیتورهای عریض حسابداری و تحلیل | حداکثر عرض محتوا (`max-w-7xl` یا `max-w-8xl`) در مرکز صفحه برای جلوگیری از کشیدگی بی‌رویه. |

---

## 🗺️ فازبندی پیاده‌سازی (Implementation Phases)

### 🔹 فاز ۱: پوسته اصلی، هدر، سایدبار و ناوبری سراسری (Shell & Navigation)
**هدف**: ساختار پایه هدر، منوی کشویی سایدبار موبایل، دکمه‌های کنترلی و سازگاری کانتینر اصلی.

- [x] **۱.۱ سایدبار واکنش‌گرا و دراور موبایل (`Sidebar.tsx`)**:
  - فعال‌سازی کامل منوی کشویی با پس‌زمینه مات تیره (`backdrop-blur-sm bg-neutral-900/60`).
  - انیمیشن ورود و خروج نرم با پشتیبانی از جهت راست‌به‌چپ (`translate-x-full` به `translate-x-0`).
  - بستن خودکار دراور موبایل پس از کلیک روی هر لینک ناوبری یا زیرمنو.
  - قفل کردن اسکرول بدنه (`overflow-hidden` روی `document.body`) هنگام باز بودن منوی موبایل.
  - نمایش مشخصات کاربر و نسخه نرم‌افزار در انتهای منوی موبایل با دسترسی لمسی سریع.
- [x] **۱.۲ هدر فشرده و هوشمند (`Header.tsx`)**:
  - تبدیل نوار ابزار هدر به چیدمان تطبیقی در موبایل:
    - پنهان‌سازی متن‌های طولانی و حفظ آیکون‌ها برای دکمه‌های وضعیت ذخیره‌سازی، همگام‌سازی و پوسته.
    - جمع‌وجور کردن انتخاب‌گر سازمان (`Organization Selector`) برای نمایش لوگو یا مخفف نام در موبایل.
    - کلید جستجوی سریع به شکل یک آیکون کوچک ذره‌بین در موبایل و کادر کامل در دسکتاپ.
    - کلید همبرگری بازکردن منو با دسترسی سریع لمسی (`min-w-[44px] min-h-[44px]`).
- [x] **۱.۳ لایه نگهدارنده اصلی (`AppShell.tsx`)**:
  - تنظیم پدینگ تطبیقی بدنه: `px-3 py-4` در موبایل، `sm:px-6 sm:py-6` در تبلت، `lg:px-8` در دسکتاپ.
  - جلوگیری از ایجاد اسکرول ناخواسته افقی در بدنه کل اپلیکیشن (`overflow-x-hidden`).

---

### 🔹 فاز ۲: کاتالوگ، محصولات، انبارداری و بارکد (Catalog & Inventory)
**هدف**: نمایش و ویرایش محصولات، تنوع‌ها، موجودی انبار و چاپ برچسب در صفحات کوچک.

- [x] **۲.۱ صفحه محصولات و تنوع‌ها (`ProductsView.tsx` & `VariantsView.tsx`)**:
  - نوار ابزار فیلترها، جستجو و دکمه «محصول جدید»: چیدمان عمودی/شناور در موبایل و افقی در دسکتاپ.
  - جدول محصولات: کانتینر اسکرول افقی نرم با نمایش ثابت ستون تصویر و نام محصول و اسکرول بقیه ستون‌ها (قیمت، موجودی، کد).
  - کلیدهای عملیات سطری (ویرایش، حذف، بارکد): تبدیل به منوی بازشونده (سه نقطه یا دکمه‌های بزرگ لمسی).
- [x] **۲.۲ نمای کلی انبار و گردش کالا (`InventoryOverviewView.tsx` & `MovementsView.tsx`)**:
  - کارت‌های آمار موجودی کل، حداقل موجودی و کسری: تبدیل از ۴ ستون به ۲ ستون در موبایل (`grid-cols-2 lg:grid-cols-4`).
  - جدول گردش انبار (ورود، خروج، انتقال): ساختار لیستی بهینه‌شده برای تبلت و انباردار با نمایش شماره سند و تاریخ شمسی.
- [x] **۲.۳ چاپ و تولید بارکد (`BarcodePrintView.tsx`)**:
  - پنل تنظیمات برچسب (ابعاد، محتویات، تعداد چاپ) و پیش‌نمایش برچسب: در موبایل به صورت تب یا چیدمان پشته‌ای عمودی قرار می‌گیرند.
  - تنظیم ابعاد برچسب‌ها با پرینترهای حرارتی و پیش‌نمایش زنده سازگار با اندازه صفحه.
- [x] **۲.۴ جدول‌های پایه و راهنمای سایز (`AttributesView`, `CategoriesView`, `SizeGuidesView`)**:
  - تنظیم فرم‌های ثبت دسته‌بندی، رنگ و سایز برای تایپ راحت در گوشی.

---

### 🔹 فاز ۳: فرآیند فروش، سفارشات و تدارکات (Sales & Procurement)
**هدف**: تسریع ثبت سفارش پای صندوق توسط تبلت و گوشی، مشاهده سوابق مشتریان و خرید کالا.

- [x] **۳.۱ صفحه ثبت فاکتور فروش (`CreateOrderView.tsx`)**:
  - لایه‌بندی اصلی: در دسکتاپ دو ستونه (انتخاب کالا در راست + سبد و تسویه در چپ)، در موبایل به صورت استپ یا تب («انتخاب اقلام» و «سبد خرید شناور در پایین»).
  - هدر واکنش‌گرا با دکمه‌های متناسب با صفحه نمایش و کنترل بهینه شیفت صندوق و پرینت.
  - چیدمان دکمه‌های روش تسویه (کارتخوان، نقدی، کارت به کارت، نسیه) متناسب با صفحات کوچک لمسی.
- [x] **۳.۲ لیست سفارشات و پرونده مشتریان (`OrdersView.tsx` & `CustomersView.tsx`)**:
  - فیلترهای وضعیت سفارش و نوار ابزار جستجو: چیدمان واکنش‌گرا با کنترل‌های فشرده و سازگار با تاچ.
  - دکمه‌های اکسل و ایجاد سفارش با برچسب‌های متناسب در موبایل و دسکتاپ.
  - مودال تسویه سریع سفارش (`SettlePaymentModal`): عرض مناسب با دکمه‌های ثبت دریافت نقدی/کارتخوان.
- [x] **۳.۳ خرید و تدارکات (`PurchaseOrdersView.tsx` & `SuppliersView.tsx`)**:
  - ثبت سفارش خرید از تأمین‌کننده، رسید کالا و فرم‌های افزودن اقلام با گرید متناسب برای نمایش در گوشی و تبلت.

---

### 🔹 فاز ۴: مجموعه حسابداری و خزانه‌داری (Financial & Accounting Suite)
**هدف**: گزارش‌های سود و زیان، دفاتر طرف حساب، حساب‌های بانکی، چک‌های صیادی و گزارشات مالیاتی در ابعاد متغیر.

- [x] **۴.۱ پیشخوان مالی و سود و زیان (`AccountingDashboardView.tsx`)**:
  - کارت‌های درآمد، هزینه‌ها، سود ناخالص و خالص: واکنش‌گرایی دو ستونه در موبایل و چهار ستونه در دسکتاپ.
  - نمودارهای روند فروش و هزینه‌ها: تنظیم خودکار نسبت تصویر (`ResponsiveContainer` ریچارتز) برای جلوگیری از لهیدگی در موبایل.
- [x] **۴.۲ مدیریت هزینه‌ها و دفاتر اشخاص (`ExpensesView.tsx` & `PersonLedgerView.tsx`)**:
  - کارت تراز حساب و وضعیت مانده‌حساب (بدهکار/بستانکار) با تایپوگرافی خوانا.
  - فیلتر بازه تاریخی شمسی با تقویم لمسی متناسب با صفحه گوشی.
- [x] **۴.۳ خزانه‌داری و چک‌های صیادی (`FinancialAccountsView.tsx` & `ChequesView.tsx`)**:
  - کارت‌های حساب‌های بانکی و صندوق‌ها به صورت گرید انعطاف‌پذیر.
  - جدول چک‌ها با برچسب‌های وضعیت رنگی (در جریان وصول، پاس شده، برگشتی) و اکشن‌های سریع.
- [x] **۴.۴ سرشکن بهای تمام‌شده، گزارشات مالیاتی و خروجی نرم‌افزارهای مالی (`LandedCostsView`, `TaxReportsPage`, `AccountingExportPage`)**:
  - جداول چندستونه مالیاتی (ارزش افزوده و گزارش فصلی ۱۶۹): اسکرول افقی روان با سربرگ چسبان.
  - دکمه‌های خروجی اکسل و CSV با چیدمان لمسی در موبایل.

---

### 🔹 فاز ۵: ووکامرس، تنظیمات سازمان، ابزارهای سراسری و تست نهایی (WooCommerce, Settings & Modals)
**هدف**: تنظیمات فروشگاه آنلاین، فرم‌های سازمانی، لایسنس ماژول‌ها و استانداردسازی تمام مودال‌ها.

- [ ] **۵.۱ صفحه اتصال و همگام‌سازی ووکامرس (`WooCommercePage.tsx`)**:
  - منوی تب‌های داخلی ووکامرس (پیشخوان، نگاشت کالاها، لاگ‌ها، تنظیمات): اسکرول افقی لمسی.
  - لاگ‌های همگام‌سازی: کادر مانیتورینگ متنی فشرده با فونت مونو در موبایل.
- [ ] **۵.۲ زیرمنوها و بخش تنظیمات (`SettingsView.tsx` و صفحات تابعه)**:
  - زیرمنوی اختصاصی تنظیمات: تب‌های ناوبری به صورت منوی کشویی یا نوار افقی اسکرول‌شونده در موبایل.
  - فرم پروفایل سازمان، اعضای تیم، نقش‌ها و ماژول‌ها: گرید فیلدها از ۲ و ۳ ستون به ۱ ستون در موبایل.
  - بخش آپلود لوگو سازمان و آواتار کاربر: نمایش بهینه در کارت فشرده با ابعاد مناسب صفحه لمسی.
- [ ] **۵.۳ حسابرسی و بهینه‌سازی تمامی مودال‌ها (Modals Audit)**:
  - `EditOrganizationModal`, `UserProfileModal`, `PurchaseModuleModal`, `UpgradeToProModal`, `UpdateNotificationModal`.
  - تنظیم `max-h-[90vh] overflow-y-auto w-full p-4 sm:p-6` برای تمامی پنجره‌ها جهت نمایش بی‌نقص در دستگاه‌های با ارتفاع کم.
- [ ] **۵.۴ تست و راستی‌آزمایی جامع در رزولوشن‌های استاندارد**:
  - موبایل آیفون و اندروید (`375px`, `390px`, `412px`).
  - تبلت آی‌پد و تبلت‌های اندرویدی (`768px`, `820px`, `1024px`).
  - دسکتاپ استاندارد و لپ‌تاپ (`1280px`, `1440px`, `1920px`).

---

## 🚀 برنامه اجرایی و اولویت‌ها

ما پیاده‌سازی را به ترتیب از **فاز ۱ (هدر، سایدبار و اسکلت اصلی برنامه)** آغاز کرده و سپس به ترتیب فازهای بعدی را با حفظ یکپارچگی کدها، عدم اختلال در عملکرد آفلاین/آنلاین و انطباق کامل با قوانین معماری TANKHOR مستقر خواهیم کرد.
